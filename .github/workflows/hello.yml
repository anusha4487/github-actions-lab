name: First Workflow
on:
  push:
jobs:
  hello-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Welcome Message
        run: echo "Hello DevOps!"
      - name: Display Date
        run: date
      - name: Display Current Directory
        run: pwd